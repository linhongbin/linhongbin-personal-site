# linhongbin-personal-site —— 工作规则

> 适用于**在本仓内工作的会话**。唯一例外：上游规划层的下发会话（它按自己的治理文件授权改 `context/goal.md` 与 todo 的下发块）。

## 文件归属（先看这条，再动手）

| 文件 | 谁写 | 你在本仓能做什么 |
|---|---|---|
| `context/goal.md` | **上游规划层（仓外）** | 👁 **只读**。不许改、不许"顺手对齐措辞"。觉得它写错了 → 写进 `status.md` 的「需要主仓决策」，等下发 |
| `context/todo.md` → `## 主仓下发` | **上游规划层** | 只能勾 `[x]`、追加 `（✅ 日期）` 或 `（⚠️ 阻塞：原因）`。⛔ 不许改措辞、不许删、不许重编号 |
| `context/todo.md` → `## 本仓自管` | **你** | ✅ 全权 |
| `context/status.md` | **你** | ✅ 全权，且是**唯一**写者。顶部 ROLLUP 块必须保持格式（上游按格式解析） |
| 代码 / 数据 / 文档 / 硬件事实 | **你** | ✅ 全权 |

`goal.md` 被本仓改动会被上游的指纹比对抓到，按越权处理。**上行只有一条通道：`status.md` 的「需要主仓决策」。**

## 边界

- ⛔ 不读、不写本仓目录以外的任何路径（含上层目录、并列的其他仓）
- ⛔ 本仓任何文件不出现仓外路径或仓外内容

## ROLLUP 块（`context/status.md` 顶部，格式别动）

```markdown
<!-- ROLLUP:BEGIN -->
最后更新: YYYY-MM-DD
健康: 🟢          # 🟢 按计划 / 🟡 有卡点但自己能解 / 🔴 卡死或需上游决策
一句话: 本仓现在在哪
当前卡点: 没有 / 具体卡在什么上
下一个里程碑: 名字 + 目标日期
需要主仓决策: 无 / 具体问题
<!-- ROLLUP:END -->
```

块以下随便写（实验记录、数据、诊断）。**每次实质进展就更新，至少每周一次。**

## 本仓公开纪律（⚠️ 本仓**已经是公开仓**，且直接对外服务 hongbinlin.com）

- `context/` 已 gitignore——**治理三件套只在本地，永不 commit**。改它们不需要也不允许 `git add context/`
- 站点与博客文字 ⛔ 不出现：**雇主名 · 传感器品牌 · 未公开的硬件路线 · 私有仓链接 · 个人职业规划**
- 每次 push 都会重建 hongbinlin.com——**push 即发布**，先自己读一遍再推
- 裸地址 `linhongbin.github.io` 已随改名永久失效，唯一入口是 `hongbinlin.com`

## 时间纪律（2026-09-01 二次重定：本仓是唯一的公有仓，最严）

**本仓可见性**：🌐 **公有** ⇒ **commit 和 push 都守窗口**（不只是 push）。

| | 时段 |
|---|---|
| ✅ 可以 `git commit` **和** `git push` | 周一至周五 **20:00 → 次日 10:00** · 周六周日**全天** |
| ⛔ 两者都不要做 | 周一至周五 **10:00 – 20:00** |

**为什么 commit 也管**：公有仓的 **author date 随历史永久公开、事后改不掉**。
窗口外只写文件、不 `git commit`；等窗口开了再一次提交。
本仓已装 **pre-commit + pre-push** 双守卫，禁窗内直接拦下；确有必要 `WINDOW_OK=1 git <命令>`。
守卫是本地 hook、不进 git——换机器或重新 clone 后需要重装（上游 `install-push-guard.sh`）。

⛔ **不要改写已有提交的 author date**——伪造的证据链是故意的证据。
（已知既成事实：`c773524` / `e3ff72c` / `a58faf6` 三个提交的时间戳落在 9/1 白天，
守卫装上之前提交的，已推出去，认了；此后不再有。）

⚠️ 本仓 `git push` ＝ 重建 hongbinlin.com ＝ 对外发布，所以时间窗对它尤其要紧：
禁推窗口内做的任何改动都只 commit、不推，等窗口开了再让它上线。
